<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.0.0/fonts/remixicon.css"
    rel="stylesheet"
/>
</head>
<body>
  <div class="w-full px-10 py-3 flex justify-between bg-zinc-800 text-white items-center">
    <h3>Pintrest</h3>
    <div class="flex gap-10 items-center">
      <a href="/profile">Profile</a> <a href="/feed">Feed</a> <a href="/logout" class="px-2 py-1  bg-red-700 text-white rounded-lg">Logout</a>

    </div>
  </div>

<div class="profile w-full min-h-screen bg-zinc-800 pt-1">

<form id="uploadform"hidden action="/fileupload" method="post" enctype="multipart/form-data"  >
  <input type="file" name="image">
</form>

  <div class="profdets flex flex-col items-center mt-20">
    <div class="relative">

<span class="w-10 h-10 hover:cursor-pointer absolute bottom-0 right-0 rounded-full flex items-center justify-center bg-zinc-200">
  <i id="uploadicon" class="text-zinc-800  ri-pencil-fill rounded-full "></i>
</span>
<div class="w-32 h-32 bg-zinc-200 rounded-full overflow-hidden">
  <img class="w-full h-full object-cover" src="/images/uploads/<%=user.profileImage%>" alt="">
</div>
    </div>
    <div class="text-white flex flex-col items-center ">
    <h1 class="text-3xl text-white font-semibold mt-5"><%=user.name%></h1>
    <h3 class="text-md">@<%=user.username%></h3>
    <a href="" class="mt-5 px-7 py-3 bg-zinc-700 rounded-full text-xs font-semibold ">Edit</a>
    <a href="/add"><button class="px-10 py-2 rounded-lg bg-red-700 text-xs font-semibold mt-5">Add New Post</button></a>
  </div>
  </div>


<div class="cards flex flex-wrap gap-10 px-10 mt-10">
<% if(user.posts.length!=0){%>
 <div class="card">
   <div class="w-52 h-40 bg-zinc-200 rounded-lg overflow-hidden">
    <img class="w-full h-full object-cover " src="/images/uploads/<%=user?.posts[0]?.image%>" alt="">
   </div>
   <a href="/show/posts" class="inline-block text-xl font-semibold mt-3 text-white">Your Pins</a>
   <h5 class="text-sm sont font-medium text-white opacity-90"><%=user.posts.length %>Pins</h5>
 </div>
<% } %>

</div>
</div>

<script>
  document.querySelector("#uploadicon").addEventListener("click",function(){
    document.querySelector("#uploadform input").click()
  })
  document.querySelector("#uploadform input").addEventListener("change",function(){
   document.querySelector("#uploadform").submit()
  })
</script>
</body>
</html>